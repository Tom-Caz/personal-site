---
const { title, subtitle, img, desc, url, duration, tags, target = "_blank" } = Astro.props;
import { Image } from "@astrojs/image/components";
---

<div
  class="rounded-lg bg-base-100 hover:shadow-xl transition ease-in-out hover:scale-[102%]"
>
  <a href={url} target={target}>
    <div class="hero-content flex-col md:flex-row">
      {
        img && (
          <Image
            src={img}
            aspectRatio="16:9"
            width={750}
            format="webp"
            alt={title}
            class="max-w-full md:max-w-[13rem] rounded-lg"
            background="white"
          />
        )
      }
      <div class="grow w-full">
        <div class="flow-root">
        <h1 class="text-xl font-bold float-left">
          {title}
        </h1>
        <span class="float-right">{duration}</span>
    </div>
        
        <div>
          <span class="text-xl mr-1"> {subtitle}</span>
        </div>
        <p class="py-1 text-1xl">{desc}</p>
        <div class="card-actions justify-end">
          {
            tags &&
              tags.map((tag) => <div class="badge badge-outline">{tag}</div>)
          }
        </div>
      </div>
    </div>
  </a>
</div>
